// we use exports when we need to export multiple things.
// we simply use the dot to add a new property to the exports object.
exports.add = (a, b) => a + b;
exports.subtract = (a, b) => a - b;
exports.multiply = (a, b) => a * b;
exports.divide = (a, b) => a / b;
